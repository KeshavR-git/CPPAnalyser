#include <iostream>
#include <vector>

class CircularLine
{
public:
	int longestTravel(std::vector<int> t) {
/*
		int largestDistance;
		int currentSum = 0;
		int test = 0;

		for (int i = 0; i < t.size(); ++i)
		{
			for (int j = 0; j < t.size(); ++j)
			{
				if (i != j)
				{
					currentSum = 0;
					test = i;
					while (test != j) {

						currentSum = currentSum + t[test];
						test = test + 1;
						if (test > t.size() - 1)
						{
							std::cout << test << " " << i << " " << j << " " << currentSum << std::endl;
							test = 0;
						}
					}
					test = 0;

				}
				if (currentSum > largestDistance)
				{
					largestDistance = currentSum;
				
				}
			}
		}

		


		return largestDistance;*/
	}
	
};