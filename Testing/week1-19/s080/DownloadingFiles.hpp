#include<iostream>
#include<vector>
using namespace std;
int XX_MARKER_XX;

class DownloadingFiles
{
	public:
	double actualTime(vector<string> tasks)
	{	
		

/*
		double res;
		vector<string> v;
		v.push_back(*tasks);
		tasks++;
		v.push_back(*tasks);
		cout<<v[0]<<" |	 "<<v[1]<<endl;
		//sstream could be useful for tokenising each v[n] 
*/
	}
};	
