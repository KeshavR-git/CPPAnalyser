#include <iostream>
#include <stdlib.h>
#include <vector>
int XX_MARKER_XX;

class PartySeats {
    public:
    std::vector<std::string> seating(std::vector<std::string> attendees) {
        std::vector<std::string> seatingorder;
        std::vector<std::string> bois;
        std::vector<std::string> grills;
        int lads = 0;
        int lasses = 0;
        
        /*for(int i = 0; i < attendees.size(); i++) {
            if(attendees[i] == " boy") {
                lads++;
            }
            else if(attendees[i] == " girl") {
                lasses++;
            }
        }
        if(lads != lasses) {
            return seatingorder;
        }*/
        return seatingorder;
    }
};